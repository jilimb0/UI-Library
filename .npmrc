# Hoist binaries from all packages to the root node_modules/.bin
# so tools like rollup, tsc, vitest are available when pnpm runs scripts
shared-workspace-lockfile=true
link-workspace-packages=true

# Make devDependency binaries (rollup, tsc, etc.) available in each package's scripts
hoist-workspace-packages=true
