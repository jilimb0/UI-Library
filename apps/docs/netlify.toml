[build]
  command = "pnpm --filter docs build"
  publish = "apps/docs/dist"

[build.environment]
  NODE_VERSION = "22"
  PNPM_VERSION = "9"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
